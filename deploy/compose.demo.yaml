name: mrba-demo
services:
  postgres:
    image: postgres:18.3-alpine@sha256:54451ecb8ab38c24c3ec123f2fd501303a3a1856a5c66e98cecf2460d5e1e9d7
    restart: unless-stopped
    environment:
      POSTGRES_DB: mrba_demo
      POSTGRES_USER: mrba_owner
      POSTGRES_PASSWORD: ${MIGRATION_DB_PASSWORD:?Required}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD:?Required}
    volumes:
      - demo_data:/var/lib/postgresql
      - ./postgres/init.sh:/docker-entrypoint-initdb.d/10-app-role.sh:ro
    networks: [database]
    healthcheck:
      test: [CMD-SHELL, 'pg_isready -U mrba_owner -d mrba_demo']
      interval: 5s
      timeout: 5s
      retries: 20
  migrate:
    image: mrba-migrate:20260922-release
    environment:
      DATABASE_URL: postgresql://mrba_owner:${MIGRATION_DB_PASSWORD}@postgres:5432/mrba_demo
    depends_on:
      postgres: {condition: service_healthy}
    networks: [database]
    restart: "no"
  api:
    image: mrba-api:20260922-release
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
      DATABASE_URL: postgresql://mrba_app:${APP_DB_PASSWORD}@postgres:5432/mrba_demo
      JWT_SECRET: ${JWT_SECRET:?Required}
      RECOVERY_EPOCH: ${RECOVERY_EPOCH:?Required}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    depends_on:
      migrate: {condition: service_completed_successfully}
    ports: ["127.0.0.1:3108:3000"]
    networks: [database, frontend]
    read_only: true
    tmpfs: [/tmp]
    security_opt: [no-new-privileges:true]
    cap_drop: [ALL]
    logging:
      driver: json-file
      options: {max-size: 5m, max-file: "2"}
networks:
  database: {internal: true}
  frontend:
volumes:
  demo_data:
